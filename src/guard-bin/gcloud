#!/bin/sh
unset NODE_OPTIONS NODE_PATH
case "$0" in */*) guard_dir=${0%/*} ;; *) guard_dir=. ;; esac
guard_dir=$(CDPATH= cd -- "$guard_dir" && /bin/pwd) || exit 127
node_bin=$(command -v node 2>/dev/null) || exit 127
exec "$node_bin" "$guard_dir/cloud-cli-guard.mjs" gcloud "$@"
